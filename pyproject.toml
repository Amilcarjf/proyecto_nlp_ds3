[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proyecto-nlp-ds3"
version = "0.1.0"
description = "Proyecto académico de clasificación comparativa de noticias"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["src"]